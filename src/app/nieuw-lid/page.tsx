/*
 * Nieuw lid
 *
 * Wat doet deze pagina:
 * Deze pagina is momenteel een lege placeholder voor het toevoegen van een nieuw lid. De echte lid-aanmaak loopt via management en het trainerdashboard.
 *
 * Data:
 * Geen directe data.
 *
 * Toegang:
 * management / trainer
 *
 * Gerelateerde API routes:
 * Geen.
 */

import { redirect } from 'next/navigation'

export default function NieuwLid() {
  redirect('/')
}
