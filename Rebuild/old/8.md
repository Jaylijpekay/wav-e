# WAV-e Rebuild — Pass 8: README

**Branch:** `main`
**Session type:** Low risk. Documentation only. No code changes.

---

## Context

WAV-e is a coaching intelligence layer for an EMS studio built on Next.js 14 (App Router, TypeScript, Tailwind CSS) with Supabase as the backend. All UI copy and DB column names are in Dutch.

This is Pass 8 of 8 — the final pass. Write `README.md` at the project root. The audience is Bart: the client, primary user, and future maintainer. He is not a developer by training but can read code with guidance. The README must let him set up a local dev environment and understand the codebase without asking for help.

Write the README in Dutch.

---

## Required sections — in this order

### 1. Wat is WAV-e?

One paragraph. Plain Dutch. No jargon.

Explain: WAV-e is een coaching-intelligentielaag voor een EMS-studio. Het helpt trainers bij het bijhouden van coachingscycli, contactmomenten en acties per lid. Managers kunnen studio-breed zien hoe het coachingswerk verloopt.

Do not mention Next.js, Supabase, or technical implementation here — this is for Bart's orientation.

### 2. Lokaal opstarten

Step-by-step instructions for running the app locally. Include:

```
1. Repository clonen
2. npm install
3. .env.local aanmaken (zie sectie hieronder)
4. npm run dev
5. Open http://localhost:3000
```

Keep it numbered, keep it short.

### 3. Benodigde omgevingsvariabelen

List every required environment variable. For each, explain in one sentence where to find it. Format:

```
NEXT_PUBLIC_SUPABASE_URL        — Supabase dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY   — Supabase dashboard → Project Settings → API → anon public key
SUPABASE_SERVICE_ROLE_KEY       — Supabase dashboard → Project Settings → API → service_role key (geheim, nooit publiek)
```

Read the actual `.env.local.example` or existing env references in the codebase to confirm the exact variable names. Do not guess.

### 4. Mappenstructuur

Explain what each folder does. Read the actual file tree before writing this. Format:

```
src/
  app/          — alle pagina's en API routes
  components/   — gedeelde UI-componenten
  lib/          — gedeelde hulpfuncties (stoplight logica, Supabase clients)
```

Expand each subfolder of `src/app/` with a one-line Dutch explanation of what it contains. For example:

```
  app/
    admin/        — beheerpagina voor supergebruiker (tokens, trainers, leden)
    console/      — tablet-login via token en PIN
    gesprek/      — nieuw evaluatiegesprek invoeren
    leden/        — ledenprofiel, evaluatie, vooruitgang
    login/        — inlogpagina
    management/   — studio-overzicht voor management
    trainer/      — trainer dashboard en ledenlijst
    api/          — server-side API routes
```

### 5. Een nieuw lid toevoegen

Step-by-step in plain Dutch. Read the actual flow in the codebase before writing this — do not guess the steps. Include which page to use and what fields are required.

### 6. Een nieuwe trainer toevoegen

Same as above. Read the admin page to confirm the actual flow.

### 7. Hoe werkt het stoplicht?

Explain the stoplight logic in plain Dutch. No code. Bart should understand this without opening `stoplight.ts`.

Base it on the comments added in Pass 7:

- Groen: contact binnen 14 dagen — lid is op koers
- Oranje: 15 tot 28 dagen — aandacht nodig
- Rood: 29 dagen of meer, of geen contact bekend — urgent

Explain that "contact" means either a contact moment or an evaluation — whichever is more recent.

### 8. Bekende aandachtspunten

List the known open items for Bart to be aware of:

- iPad Safari login (session cookie) is not device-tested — Bart must verify login on the actual iPad as first step
- Momentum strip (trainer welkomstscherm) is planned for v0.1.1 — not yet built
- QR code op token aanmaken is planned for v0.1.1 — not yet built
- v0.2 integraties (Onlineafspraken.nl, Google Calendar, WhatsApp Business API) zijn gepland na stabilisatie van v0.1

---

## Constraints

- Write entirely in Dutch
- No code blocks except for the env vars and folder structure sections
- No technical jargon in sections 1, 5, 6, 7 — Bart reads those without a developer next to him
- Read the actual codebase for sections 3, 4, 5, 6 — do not invent steps or variable names
- Keep it under 400 lines — if it gets longer, you are over-explaining

---

## Output

Write `README.md` to the project root. Then produce a one-line summary confirming it was written and how many sections it contains.

Then stop.

---

## Final gate — before this pass is complete

1. `npm run build` zero errors
2. Reconnect Vercel → GitHub webhook (do this manually before deploying)
3. Push README to `main` — verify Vercel deployment triggers automatically
4. Open production URL — verify all three auth paths work: admin, management, trainer console
5. Open Bart's member record (WE-002) — confirm contact status is correct
6. Read the README yourself — if Bart can set up local dev from it without asking you, it is done

---

*WAV-e Rebuild Plan | Serendipity Projects | Mei 2026*
*Acht passes. Acht gates. Klaar.*

